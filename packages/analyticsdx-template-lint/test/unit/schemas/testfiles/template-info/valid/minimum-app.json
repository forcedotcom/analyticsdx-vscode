{
  // templateType defaults to 'app' if left off
  "label": "MinTemplate",
  "name": "MinTemplate",
  "assetVersion": 47.0,
  "releaseInfo": {
    "templateVersion": "1.1"
  }
  // app template's technically need 1 dashboard, dataset, or dataflow, but that's checked by the linter
  // and not the schema
}
